---
dataset:test:
  type: sql
  name: test
  query: "SELECT \r\n        id,\r\n        sobject,\r\n        stage_name,\r\n        gclid_c,\r\n
    \       conversion_rating,\r\n        cet_timestamp::timestamp\r\nFROM marketing.dm_google_ads_conversion_rating_enhanced\r\nWHERE
    1=1\r\nAND cet_timestamp::timestamp  >= (current_date - 14)\r\nAND gclid_c is
    not null \r\nORDER BY cet_timestamp::timestamp desc"
  description:
  icon:
  connection: connection:redshift-prod-emnify-dwh
